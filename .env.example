# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=callanalytics
DB_USER=admin
DB_PASSWORD=your_database_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS Configuration
# For production, replace with your domain
CORS_ORIGIN=*

# NocoDB Configuration (if using NocoDB)
NOCODB_API_URL=https://your-nocodb-instance.com/api/v1
NOCODB_API_TOKEN=your_nocodb_api_token
NOCODB_PROJECT_ID=your_project_id
NOCODB_TALKTIME_TABLE_ID=your_talktime_table_id
NOCODB_IDLE_SESSIONS_TABLE_ID=your_idle_sessions_table_id